# Regression Test Profile
# Comprehensive testing with full coverage
# Usage: PROFILE=regression npm run test:qa

# Test execution
TIMEOUT=60000
RETRIES=2
PARALLEL_THREAD=2
HEADLESS=true

# Reporting
VIDEO=retain-on-failure
SCREENSHOT=only-on-failure
TRACE=retain-on-failure
SLOW_MO=0
