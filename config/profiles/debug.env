# Debug Profile
# Visible browser with full logging
# Usage: PROFILE=debug npm run test:qa

# Test execution
TIMEOUT=120000
RETRIES=0
PARALLEL_THREAD=1
HEADLESS=false
SLOW_MO=500

# Reporting (full capture for debugging)
VIDEO=on
SCREENSHOT=on
TRACE=on
